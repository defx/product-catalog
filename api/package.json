{
  "name": "moneybox-product-catalog-api",
  "version": "1.0.0",
  "description": "Moneybox Product Catalog - API",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "contentful space migration --space-id=$CONTENTFUL_SPACE_ID --environment-id=$CONTENTFUL_ENVIRONMENT --access-token=$CONTENTFUL_MANAGEMENT_TOKEN"
  },
  "dependencies": {
    "contentful": "^11.8.11",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/node": "^24.10.0",
    "contentful-cli": "^3.9.12",
    "contentful-management": "^11.62.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
